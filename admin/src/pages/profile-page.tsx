import { useForm } from 'react-hook-form';
import { Button, Card, Input } from '../components/ui';
import { useAuth } from '../hooks/use-auth';
export function ProfilePage(): React.JSX.Element { const { user } = useAuth(); const { register, handleSubmit } = useForm({ defaultValues: { name: user?.name ?? '', email: user?.email ?? '' } }); return <><div className="mb-7"><h2 className="text-2xl font-bold">My Profile</h2><p className="mt-1 text-[#806c75]">Manage your administrator details.</p></div><Card className="max-w-xl"><form onSubmit={handleSubmit(() => undefined)} className="space-y-5"><label className="block text-sm font-semibold">Name<Input className="mt-2" {...register('name')}/></label><label className="block text-sm font-semibold">Email<Input className="mt-2" type="email" {...register('email')}/></label><p className="text-sm text-[#806c75]">Role: <span className="font-semibold text-rose">{user?.role}</span></p><Button type="submit">Save changes</Button></form></Card></>; }
