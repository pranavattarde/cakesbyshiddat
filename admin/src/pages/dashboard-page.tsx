import { CakeSlice, CalendarDays, Images, Mail, MessageSquareHeart } from 'lucide-react';
import { motion } from 'framer-motion';
import { Card } from '../components/ui';

const metrics = [['Total Cakes', CakeSlice, '#f9e4ea'], ['Gallery Images', Images, '#eeeafb'], ['Total Events', CalendarDays, '#e5f4ee'], ['Testimonials', MessageSquareHeart, '#fff1dc'], ['Unread Messages', Mail, '#f9e4ea']] as const;
export function DashboardPage(): React.JSX.Element { return <><div className="mb-7"><h2 className="text-2xl font-bold">A sweet overview</h2><p className="mt-1 text-[#806c75]">Your bakery’s content and customer activity at a glance.</p></div><div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-5">{metrics.map(([label, Icon, tone], index) => <motion.div key={label} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:index*.06}}><Card><div className="flex items-center justify-between"><div><p className="text-sm text-[#806c75]">{label}</p><p className="mt-3 text-3xl font-bold">—</p><p className="mt-1 text-xs text-[#9b838d]">Awaiting API data</p></div><span style={{background:tone}} className="rounded-2xl p-3 text-[#7669a6]"><Icon className="h-6 w-6"/></span></div></Card></motion.div>)}</div><Card className="mt-6"><h3 className="font-bold">Welcome to your dashboard</h3><p className="mt-2 max-w-2xl text-sm leading-6 text-[#806c75]">Your dashboard is connected to the production API. Add content from the resource areas as endpoints become available.</p></Card></>; }
