import { Injectable, NotFoundException } from '@nestjs/common'; import { Prisma } from '@prisma/client'; import { PrismaService } from '../prisma/prisma.service'; import { CreateTestimonialDto, TestimonialQueryDto, UpdateTestimonialDto } from './dto/testimonial.dto'; import { PaginatedResponse, paginationMeta } from '../common/dto/pagination-query.dto';
@Injectable() export class TestimonialsService {constructor(private readonly prisma:PrismaService){} create(dto:CreateTestimonialDto){return this.prisma.testimonial.create({data:dto})} async findAll(q:TestimonialQueryDto):Promise<PaginatedResponse<Prisma.TestimonialGetPayload<object>>>{const where:Prisma.TestimonialWhereInput={...(q.featured===undefined?{}:{featured:q.featured}),...(q.search?{OR:[{name:{contains:q.search,mode:'insensitive'}},{review:{contains:q.search,mode:'insensitive'}}]}:{})};const[data,total]=await this.prisma.$transaction([this.prisma.testimonial.findMany({where,skip:(q.page-1)*q.limit,take:q.limit,orderBy:{createdAt:q.sortOrder}}),this.prisma.testimonial.count({where})]);return{data,meta:paginationMeta(q.page,q.limit,total)}} async findOne(id:string){const item=await this.prisma.testimonial.findUnique({where:{id}});if(!item)throw new NotFoundException('Testimonial not found');return item} async update(id:string,dto:UpdateTestimonialDto){await this.findOne(id);return this.prisma.testimonial.update({where:{id},data:dto})} async remove(id:string){await this.findOne(id);await this.prisma.testimonial.delete({where:{id}})} async toggleFeatured(id:string){const item=await this.findOne(id);return this.prisma.testimonial.update({where:{id},data:{featured:!item.featured}})} }
