import { useState } from 'react'; import Footer from '../../components/Footer/Footer'; import Navbar from '../../components/Navbar/Navbar'; import SEO from '../../components/SEO'; import { CakeGrid } from '../../components/Cakes/CakeGrid'; import { useCakeCategories, useCakes } from '../../hooks/use-cakes';
const Cakes = () => { const [search, setSearch] = useState(''); const [category, setCategory] = useState(''); const [sort, setSort] = useState<'displayOrder' | 'createdAt' | 'name'>('displayOrder'); const [page, setPage] = useState(1); const categories = useCakeCategories(); const cakes = useCakes({ page, limit: 9, search, category: category || undefined, sort, sortOrder: sort === 'name' ? 'asc' : 'desc' }); return <><SEO title="Cakes" description="" path="/cakes" /><Navbar /><main className="bg-[#fff8f2] px-6 pb-24 pt-32"><div className="container-custom"><h1 className="text-5xl text-[#3a2d28]" style={{ fontFamily: 'Playfair Display' }}>Cake Collection</h1><div className="mt-8 grid gap-3 md:grid-cols-3"><input value={search} onChange={(event) => { setSearch(event.target.value); setPage(1); }} placeholder="Search cakes" className="rounded-xl border border-[#eadde1] bg-white px-4 py-3" /><select value={category} onChange={(event) => { setCategory(event.target.value); setPage(1); }} className="rounded-xl border border-[#eadde1] bg-white px-4 py-3"><option value="">All categories</option>{categories.data?.map((item) => <option key={item.id} value={item.slug}>{item.name}</option>)}</select><select value={sort} onChange={(event) => setSort(event.target.value as typeof sort)} className="rounded-xl border border-[#eadde1] bg-white px-4 py-3"><option value="displayOrder">Featured order</option><option value="createdAt">Newest</option><option value="name">Name</option></select></div>{cakes.isLoading ? <div className="mt-10 grid grid-cols-3 gap-7">{[1, 2, 3].map((item) => <div key={item} className="aspect-[4/5] animate-pulse rounded-3xl bg-[#f3e5dc]" />)}</div> : cakes.isError ? <div className="mt-10 text-center"><p>Unable to load cakes.</p><button onClick={() => void cakes.refetch()} className="mt-4 rounded-full bg-[#d7a88c] px-5 py-3 text-white">Retry</button></div> : <><div className="mt-10"><CakeGrid cakes={cakes.data?.items ?? []} /></div><div className="mt-10 flex justify-center gap-4"><button disabled={page === 1} onClick={() => setPage((value) => value - 1)} className="rounded-full border px-5 py-2 disabled:opacity-40">Previous</button><button disabled={page === (cakes.data?.pagination.totalPages ?? 1)} onClick={() => setPage((value) => value + 1)} className="rounded-full bg-[#d7a88c] px-5 py-2 text-white disabled:opacity-40">Next</button></div></>}</div></main><Footer /></>; }; export default Cakes;
