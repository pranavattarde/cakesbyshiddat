import { Link } from 'react-router-dom';
import type { Cake } from '../../services/cakes.service';
export function CakeGrid({ cakes }: { cakes: Cake[] }): React.JSX.Element { return <div className="grid gap-7 sm:grid-cols-2 lg:grid-cols-3">{cakes.map((cake) => <Link key={cake.id} to={`/cakes/${cake.slug}`} className="group overflow-hidden rounded-[28px] bg-white shadow-lg"><img src={cake.coverMedia.secureUrl} alt={cake.coverMedia.alt || cake.name} className="aspect-[4/3] w-full object-cover transition duration-500 group-hover:scale-105" /><div className="p-5"><p className="text-xs font-semibold uppercase tracking-widest text-[#d7a88c]">{cake.category.name}</p><h2 className="mt-2 text-2xl text-[#3a2d28]" style={{ fontFamily: 'Playfair Display' }}>{cake.name}</h2><p className="mt-2 line-clamp-2 text-sm text-[#8a7a72]">{cake.shortDescription}</p></div></Link>)}</div>; }
